
services:
  - type: web
    name: artmintflow-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 8000"
    envVars:
      - key: NFT_STORAGE_API
        value: your_nft_storage_api_key
